before_script:
  - git config --global --add safe.directory "C:/GitLab-Runner/builds/A2X5hgzdb/0/I525452/torquehubfe"

stages:
  - build
  - dockerize

build:
  stage: build
  script:
    - $env:CI = "false"
    - npm install --legacy-peer-deps
    - npm run build
    - npm ci

dockerize:
  stage: dockerize
  needs: 
    - job: build
  script:
    - $registry = "$env:CI_REGISTRY_IMAGE"
    - docker build -t my-react-app .